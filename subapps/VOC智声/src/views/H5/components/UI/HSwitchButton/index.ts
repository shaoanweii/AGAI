/**
 * SwitchButton 组件导出
 */

import HSwitchButton from './index.vue'

// 导出组件
export { default as HSwitchButton } from './index.vue'

// 导出类型
export type { HSwitchOption, HSwitchButtonProps, HSwitchButtonEmits } from './types'

// 默认导出
export default HSwitchButton
