<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voc.service.insights.engine.mapper.InsProvinceAreaMapper">

    <resultMap type="com.voc.service.insights.engine.entity.InsProvinceAreaEntity" id="InsProvinceAreaMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="areaCode" column="area_code" jdbcType="VARCHAR"/>
        <result property="areaName" column="area_name" jdbcType="VARCHAR"/>
        <result property="provinceCode" column="province_code" jdbcType="VARCHAR"/>
        <result property="provinceName" column="province_name" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into ins_province_area(area_code, area_name, province_code, province_name)
        values
        <foreach collection="model" item="entity" separator=",">
            (#{entity.areaCode}, #{entity.areaName}, #{entity.provinceCode}, #{entity.provinceName})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into ins_province_area(area_code, area_name, province_code, province_name)
        values
        <foreach collection="model" item="entity" separator=",">
            (#{entity.areaCode}, #{entity.areaName}, #{entity.provinceCode}, #{entity.provinceName})
        </foreach>
        on duplicate key update
        area_code = values(area_code) ,
        area_name = values(area_name) ,
        province_code = values(province_code) ,
        province_name = values(province_name)
    </insert>

</mapper>

