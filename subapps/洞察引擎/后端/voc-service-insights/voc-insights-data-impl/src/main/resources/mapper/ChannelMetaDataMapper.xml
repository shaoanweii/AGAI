<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voc.service.insights.engine.data.mapper.ChannelMetaDataMapper">

    <select id="findRawData" resultType="com.voc.service.insights.engine.data.entity.ChannelMetaDataEntity">
        select
       cmdv.id,
       cmdv.one_id,
       cmdv.channel_biz,
       cmdv.channel_dc,
       cmdv.data_id,
       cmdv.create_time,
       cmdv.biz_create_time,
       cmdv.biz_update_time,
       cmdv.delivery_at_time,
       cmdv.content,
       cmdv.customer_name,
       cmdv.customer_sex,
       cmdv.customer_phone,
       cmdv.license_plate,
       cmdv.car_series_code,
       cmdv.dealership_code_delivery,
       cmdv.dealership_code_purchase,
       cmdv.dealership_code_return,
       cmdv.ext_attrs,
       cmdv.ext_attrs2
       from ods_channel_meta_data_view cmdv
        left join dwd_voc2_processing_data_record ocer on cmdv.data_id = ocer.data_id
        where
        ocer.create_time >= date(#{start}) and ocer.create_time &lt; date_add(#{start},1)
        <if test="status !=null and status !='' ">
            and ocer.status = 1
            and ocer.error_code = #{errorCode}
        </if>
    </select>
</mapper>
