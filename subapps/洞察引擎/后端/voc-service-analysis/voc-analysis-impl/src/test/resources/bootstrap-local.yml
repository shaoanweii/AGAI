spring.application.name: voc-app-analysis
server.port: 8080

spring:
  cloud.nacos:
    config:
      enabled: false
    discovery:
      enabled: false
  config:
    location: classpath:${spring.profiles.active}/common.yml,classpath:${spring.profiles.active}/common-security-client.yml,classpath:${spring.profiles.active}/voc-analysis-starrocks.yml,classpath:${spring.profiles.active}/voc-analysis-redis.yml,classpath:${spring.profiles.active}/common-swagger.yml,classpath:${spring.profiles.active}/common-security-client.yml,classpath:${spring.profiles.active}/voc-analysis-kafka.yml,classpath:${spring.profiles.active}/voc-analysis-service.yml


logging:
  config: classpath:${spring.profiles.active}/logback-spring.xml
