# AGAI 完整系统运行说明

本代码包包含 AGAI 门户、统一登录与智能工作台，以及以下三个已接入子应用的完整源码：

- 消费者智调：`subapps/AI消费者2`
- 声音洞察引擎：`subapps/洞察引擎`
- VOC 智声：`subapps/VOC智声`

三个工作台入口均使用当前项目内的 `/apps/*` 构建产物，不依赖原开发者电脑上的兄弟目录。

## 环境要求

- macOS、Linux 或 Windows（建议使用 macOS/Linux，Windows 使用 PowerShell 或 Git Bash）
- Node.js `22.13.0` 或更高版本
- npm `10` 或更高版本

## 最快启动方式

代码包已经包含最近一次验证通过的 `public/apps/` 子应用产物。只需安装门户依赖即可启动完整演示系统：

```bash
npm ci
npm run dev
```

浏览器访问：<http://localhost:3000>

演示账号：

```text
账号：admin
密码：FT2026
```

## 修改子应用后的完整启动方式

首次使用或锁文件发生变化后，分别安装四个项目的依赖：

```bash
npm ci
npm ci --prefix "./subapps/AI消费者2"
npm ci --prefix "./subapps/洞察引擎/前端"
npm ci --prefix "./subapps/VOC智声"
```

重新构建三个子应用并启动门户：

```bash
npm run dev:ecosystem
```

也可以只重新构建子应用：

```bash
npm run build:subapps
```

构建产物位置：

- `public/apps/consumer`
- `public/apps/insight`
- `public/apps/voc`

## 生产构建

```bash
npm run build
npm run start
```

## 验收路径

1. 打开首页，点击“进入工作台”。
2. 使用 `admin / FT2026` 登录。
3. 依次打开“VOC智声”“消费者智调”“声音洞察引擎”。
4. 三个应用均应从当前服务域名加载，不应跳转到外部开发地址。

尚未提供源码的智能问数、KOC智营、线索运营、业务运营中台和用户洞察引擎会显示“敬请期待”。

## 洞察引擎后端

统一门户的演示模式不要求单独启动洞察引擎后端。需要联调完整后端时，请阅读：

```text
subapps/洞察引擎/LOCAL_RUNBOOK.md
```

## 交付包说明

为了保证代码包可分享且不携带本机状态，压缩包不包含：

- `.git` 版本历史
- `node_modules`
- `.vinext`、`.wrangler`、`.next` 等本机缓存
- Playwright 截图与测试临时目录
- 日志、临时输出和编译缓存

依赖版本由各目录的 `package-lock.json` 固定，可通过 `npm ci` 恢复。
